apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opt-signed-chart.fullname" . }}-test-connection"
  labels:
    {{- include "opt-signed-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opt-signed-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
